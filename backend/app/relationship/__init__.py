"""Relationship-role registry for the reminiscence companion (issue #51).

Exposes the visible relationship roles (Study 1 R1–R8) as descriptive
``RoleProfile`` data. These are relationship *functions*, not autonomous agents,
and never impersonate real people or the deceased (CLAUDE.md §7, §8).
"""
